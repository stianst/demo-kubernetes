apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: keycloak
spec:
  rules:
  - http:
      paths:
      - path: /auth
        backend:
          serviceName: keycloak
          servicePort: 8080

